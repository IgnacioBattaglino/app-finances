# app-finances

PWA de finanzas personales con enfoque FIRE (Financial Independence, Retire Early). Herramienta de uso real y pieza de portfolio. Multiusuario con registro semi-cerrado (las cuentas las crea el administrador; sin signup público). El frontend nunca envía user_id: lo completa la base con default auth.uid(), y RLS garantiza el aislamiento.

## Mapa del proyecto

Actualizá esta sección cuando agregues o muevas un archivo importante. Es un índice hacia el código, no una explicación — para arquitectura y decisiones ver ARCHITECTURE.md y FUNCTIONAL.md. Nunca cites números de línea acá: envejecen mal y quedan mintiendo.

- **Auth**: `hooks/useAuth.jsx` (sesión, recuperación) · `lib/supabase.js` (parseo del link de recovery) · `pages/Login.jsx` · `pages/ResetPassword.jsx` (nueva contraseña) · `components/ProtectedRoute.jsx`.
- **Capa de datos** (caché compartida y persistida en `localStorage`): `lib/queryClient.js` (el `QueryClient`, el persister, `isWriteRequest` e `invalidateUserData` — toda escritura a Supabase invalida todo lo del usuario, sin mapa fino de "qué toca qué") · `hooks/useCategories.js` (mismo patrón que `hooks/useAccounts.js`, ver "Cuentas del disponible") · `hooks/useLiquid.js`, `hooks/useDebts.js`, `hooks/useCards.js`, `hooks/useMovements.js` (movimientos del período, con la llave del período), `hooks/useAccountBalances.js` (Mi plata), `hooks/useExpenses.js` (el bloque de gastos de Inicio) y `hooks/usePortfolioSeries.js` (la curva de evolución) — todos con el mismo criterio: `loading` es "no hay nada que mostrar todavía", nunca "está refrescando", y un Map que haga falta se arma en el `select` de la consulta, nunca en la función que la trae (no sobrevive el paso por `localStorage`).
- **Invitaciones** (registro por link, ver ADR-017): `lib/invitations.js` (alta/baja, validación, link) · `hooks/useIsAdmin.js` · `pages/Register.jsx` (pantalla pública `/registro`) · `pages/settings/Invitations.jsx` (administración, visible solo para el admin).
- **Inversiones / contributions** (pestaña "Inversiones", antes "Portafolio"): `pages/Portfolio.jsx`, `components/AssetGroup.jsx` (lista) · `pages/AssetDetail.jsx` (detalle de activo) · `pages/settings/AssetTypes.jsx` (lista de grupos) / `AssetTypeDetail.jsx` (detalle de grupo) · `components/AssetFormModal.jsx` (alta/edición de activo) · `components/ContributionFormModal.jsx`, `components/contribution/{TransferFormModal,LiquidatePositionModal,QuantityAmountField,ExchangeRateField}.jsx` (aportar/retirar/transferir/liquidar) · `lib/portfolio.js` (cálculo, guardas, orden) · `lib/portfolioSort.js` (orden persistido) · `lib/contributions.js` (`getAllContributions`, paginada, para el portafolio entero — distinta de `getContributions({ assetId })`, acotada a un activo y sin paginar), `lib/assets.js`, `lib/assetTypes.js` · `hooks/usePortfolio.js` (las cuatro consultas + los precios en vivo aparte, sin persistir; también exporta `assetTypesKey` para compartir esa caché sin pedir todo lo demás) y `useArchivedAssets`/`useArchivedAssetTypes` en el mismo archivo. El detalle de un activo (`AssetDetail.jsx`) arranca con el nombre, el valor y el grupo de esta misma caché — su historial y las valuaciones manuales son consultas propias, paginadas. La evolución del portafolio (`components/PortfolioEvolutionChart.jsx`, ver "Dashboard") vive debajo de la tarjeta del total, siempre montada acá (a diferencia de Inicio, que solo la monta en desktop).
- **Movimientos / transactions**: `pages/Movements.jsx` (mezcla transactions y contributions, incluidas las cuentas de ahorro; el resumen del mes —los cinco renglones, "Gastos por categoría" y la serie de 12 meses— vive plegado en el celular dentro de `MonthSummary`, siempre abierto en desktop vía `hooks/useIsDesktop.js`; `ArrowAmount`/`MovedTotalRow` son el lenguaje de "plata que cambió de lugar": flecha sin signo, nunca rojo) · `hooks/useMovements.js` (el período navegado, la llave de la consulta) · `components/TransactionFormModal.jsx` (incluye la grilla de categorías más usadas, ver "Categorías") · `lib/transactions.js`, `lib/movements.js` (`monthTotals`: los cinco renglones del período —Gastos, Ingresos, Invertido, Ahorrado, "Te sobró"— y el único lugar que decide qué es Invertido y qué es Ahorrado; `contributionLabel` dice "Ahorro"/"Retiro de ahorro" para un activo convertido en cuenta de ahorro) · `lib/movementList.js` (cómo se arma la LISTA: el apareo de las dos patas de una transferencia y de los repartos de un conteo en una sola línea, y en qué cajón cae cada una) · `components/form/FilterChips.jsx` (los seis filtros) · `lib/dateRange.js` + `components/movements/RangeSheet.jsx` (qué período se mira: mes, año, todo o dos fechas) · `lib/pagination.js` (el corte silencioso de PostgREST en 1000 filas, en un solo lugar) · `components/ExpensesYearChart.jsx` (la serie de gastos de 12 meses en dólares, se mudó de Inicio; carga diferida vía `components/dashboardCharts.js`).
- **Categorías**: `pages/settings/Categories.jsx` (lista, alta, reordenar) · `components/settings/ReorderableRows.jsx` (el arrastre, compartido con Cuentas) · `pages/settings/CategoryDetail.jsx` (renombrar) · `lib/categories.js` (borrado real u oculta; `getCategoryUsage`/`topCategories`: cuántas veces se usó cada una en los últimos 90 días y la elección de las seis para la grilla del formulario de gasto/ingreso) · `hooks/useCategoryUsage.js` (ese uso, sobre la caché compartida) · `components/form/CategoryGrid.jsx` (la grilla) · `lib/systemCategories.js` (qué significa cada categoría del sistema: el único lugar que decide si una fila es un gasto real o solo plata que cambió de lugar).
- **Instrumentos / precios**: `lib/instruments.js` (catálogo, búsqueda) · `lib/portfolioPrices.js` (precio por activo) · `lib/prices.js` (Binance/CoinGecko/MEP) · `components/asset/InstrumentPicker.jsx` (buscador) · `supabase/functions/refresh_prices/` (cron).
- **Cálculo del disponible**: `lib/liquid.js` (líquido, desglose por cuenta, reconciliación, `planReconciliation`: el neteo de un conteo —la misma regla que aplica la base al escribir— y el borrado de un conteo entero; `batchesByTransaction` es la función pura que arma el Map de qué conteo escribió cada movimiento) · `hooks/useLiquid.js` (envuelve `computeCurrentLiquid`, lo usa Inicio) · `components/LiquidModal.jsx` (modal, se abre desde Dashboard).
- **Monedas**: `lib/currencyTotals.js` (la moneda local, la regla de conversión con tasa congelada, y qué líneas se muestran) · `components/MoneyStack.jsx` (el monto protagonista de una tarjeta, una línea por moneda) · `lib/localCurrency.js` (el único lugar que convierte a dólares para mostrar).
- **Cuentas del disponible** (pestaña "Mi plata"): `lib/liquidAccounts.js` (alta, orden, baja con reasignación) · `hooks/useAccounts.js` (las carga para los formularios, sin las de ahorro) · `hooks/useAccountBalances.js` (todas las cuentas CON saldo, para la pantalla, incluido `hasMovements` por cuenta; comparte llave de caché con `useAccounts`) · `components/form/AccountField.jsx` (selector + alta al vuelo) · `pages/settings/Accounts.jsx` (el total del disponible arriba de todo, con `hooks/useLiquid.js` — mismo número que "Dinero disponible" de Inicio) / `AccountDetail.jsx` (gestión) · `lib/accountTransfers.js` + `components/account/AccountTransferModal.jsx` (transferencia entre cuentas) · `components/account/SavingsMovementModal.jsx` (aportar/retirar de una cuenta de ahorro).
- **Dashboard** (Inicio, una sola pantalla en el celular): `pages/Dashboard.jsx` — el recordatorio y una tarjeta con los tres mundos como filas (`WorldRow`, mismo peso, `--text-title2`) con el Total en dólares como pie (`TotalFoot`), y debajo la tarjeta de gastos del mes (`components/ExpensesBlock.jsx`, usa `hooks/useExpenses.js`, sin gráfico). En desktop se suma la evolución del portafolio al lado, montada solo ahí (`hooks/useIsDesktop.js`, nunca `hidden`) para que el celular no descargue Recharts — mismo hook que usa Movimientos para el resumen plegable, que en desktop no pliega. `components/dashboardCharts.js` (carga diferida, compartida con Inversiones y Movimientos) → `components/PortfolioEvolutionChart.jsx` (usa `hooks/usePortfolioSeries.js`) y `components/ExpensesYearChart.jsx` (la serie de 12 meses, en Movimientos).
- **A pagar** (pestaña, ruta `/compromisos` — el nombre visible cambió, la ruta no; ver ADR-019): `pages/Commitments.jsx` (índice: tarjetas, suscripciones y deudas) · `pages/CommitmentDetail.jsx` (un plan y sus vencimientos, terminar y eliminar) · `pages/CardDetail.jsx` (una tarjeta y sus compras) · `lib/commitmentSchedule.js` (el único lugar que calcula QUÉ VENCE Y CUÁNDO — un pendiente no es una fila en ninguna tabla) · `lib/commitments.js` (consulta y escritura de planes y cargos; `groupChargesByPlan` es la función pura que arma el Map por plan) · `lib/paymentCards.js` (incluye `CARD_COLORS`, la paleta de una tarjeta, propia y distinta de la de los grupos de activos) · `components/commitments/{CommitmentReminder,DueReminder,ConfirmChargeModal,CommitmentFormModal,CardFormModal,PaymentCardVisual}.jsx` · `hooks/useCommitments.js` (también `useDuePayments`), `hooks/useCards.js`. Las etiquetas de estos formularios son sustantivos, no preguntas — excepción acotada a esta sección, ver el comentario en `CommitmentFormModal.jsx`.
- **Deudas** (dentro de A pagar): `pages/Debts.jsx` · `components/DebtFormModal.jsx`, `components/DebtPaymentModal.jsx` · `lib/debts.js` · `hooks/useDebts.js`.
- **Ajustes**: `pages/settings/SettingsHome.jsx` (índice, con el email y Cerrar sesión al pie) → `Appearance.jsx`, `Categories.jsx`/`CategoryDetail.jsx`, `ExportData.jsx` · `components/settings/{SettingsList,SettingsPage}.jsx` (piezas compartidas).
- **Formularios compartidos**: `components/FormSheet.jsx` (chrome del modal: arma el `<form>` y el botón de guardar, dueño de su propia presencia — entra/sale con `lib/spring.js`, se agarra con el dedo) · `lib/spring.js` (el resorte amortiguado y `project`, sin dependencias — la primitiva de todo movimiento guiado por un gesto) · `components/form/{CollapsedDateField,BinaryChoice,Switch,FormError,MissingHint,ConfirmAction,InlineCreate}.jsx` (`FormError` exporta también `ErrorNotice`, el aviso de "no se pudo cargar + Reintentar"; `ConfirmAction` es toda confirmación de eliminar/archivar; `InlineCreate` toda alta en el lugar) · `components/Toast.jsx` (`showToast`, el aviso de guardado) · `lib/errors.js` (qué parte de un error puede leer una persona: el único lugar que lo decide).
- **Piezas de pantalla**: `components/Layout.jsx` (pestañas y columna lateral) · `components/PageHeader.jsx` (título grande, la barra fija del celular al scrollear, y el volver de toda subpantalla) · `hooks/useGoBack.js` (a dónde vuelve un "volver": de verdad si hay historia, si no reemplaza a la madre — `backTarget` es la función pura, testeada aparte) · `lib/navHistory.js` (el pathname anterior, para la única excepción de `useGoBack`) · `components/Icons.jsx` (todos los íconos) · `components/AppLoading.jsx` (el anillo de arranque, mientras se lee la sesión — cada pantalla con datos dibuja su propio esqueleto con la clase `placeholder`, no hay un esqueleto genérico) · `App.jsx` (router de datos: rutas con carga diferida y `handle.ownBottomBar`).

## Stack
- React + Vite
- Tailwind CSS
- Supabase (PostgreSQL) como backend y base de datos
- PWA instalable en móvil
- Deploy en Vercel

## Contexto de negocio
- Tres mundos separados que nunca se suman en un "patrimonio total": dinero líquido (ARS, operativo), invertido (USD, con rendimiento) y deudas (saldo restante en USD).
- Monedas: gastos, ingresos y líquido en ARS; inversiones y deudas en USD. Cada aporte congela su tipo de cambio (MEP) del día.
- Registra: movimientos individuales (ingresos/gastos), aportes a inversión, reconciliaciones del líquido, y un portafolio de activos (CEDEARs, Renta fija, Money market, Bitcoin, Efectivo USD).
- El modelo de datos completo está en @docs/ARCHITECTURE.md (leelo cuando trabajes en base de datos, esquema o queries). El diseño funcional y el estado de cada sección (implementado vs. pendiente) está en docs/FUNCTIONAL.md.

## Reglas
- NUNCA commitear secretos ni datos financieros reales. Las claves van en .env (ya está en .gitignore).
- Los datos reales viven solo en Supabase, nunca en el repo.
- Las credenciales del usuario test viven en `.env.test.local` (ignorado por git, cubierto por `*.local`). Nunca escribas credenciales (reales o de ejemplo con pinta real) en código fuente, tests commiteados, documentación, mensajes de commit ni output de consola. Si necesitás loguearte para probar, leelas siempre de ese archivo vía variables de entorno.
- Prioridad del proyecto: que yo entienda el código. Antes de cambios grandes, explicá el plan y esperá mi OK.
- Commits en formato Conventional Commits (feat:, fix:, chore:, docs:).
- Español para explicaciones; código y nombres de variables en inglés.
- Las respuestas a Nacho en el chat son siempre en español rioplatense, incluso en este proyecto de código y commits en inglés.

## Sistema visual
El lenguaje es el de una app de iOS: fondo agrupado gris frío, tarjetas SIN marco (la jerarquía la da el contraste con el fondo, más una sombra mínima), separadores internos sangrados desde el texto, y tipografía del sistema — que en iPhone y Mac resuelve a San Francisco. Ninguna webfont.

- **Todo el color vive en `src/index.css`** como variables de `@theme`. El modo oscuro NO reescribe clases: redefine esas mismas variables en `[data-theme="dark"]` y en el `@media (prefers-color-scheme: dark)`. Un componente que usa los tokens (`bg-card`, `text-ink-soft`, `border-line`, `bg-mist`) ya funciona en los dos modos sin tocarlo. Nunca hardcodear un hex en un componente.
- Tres niveles de texto: `ink` (principal), `ink-soft` (secundario) e `ink-faint` (terciario). Superficies: `paper` (fondo), `card` (tarjeta), `mist` (relleno de un control sobre una tarjeta) y `lift` (la pieza que se apoya ENCIMA de `mist`, ej. la pastilla del segmentado — no puede ser `card`, que en oscuro es más oscuro que `mist`). `scrim` es el velo del modal, oscuro en los dos modos.
- El acento son DOS tokens porque un color no hace los dos trabajos: `accent` rellena botones (siempre con blanco encima, así que es oscuro y pasa AA) y `accent-ink` es el acento como TEXTO sobre el fondo de la app — en claro son el mismo, en oscuro `accent-ink` se aclara. Ver `src/lib/theme.js`.
- **La regla del rojo (bloque 10): `clay` es plata que se fue, o algo que no se pudo hacer** — una pérdida (rendimiento negativo), el total de "Gastos" en los renglones del mes y el signo de un gasto en una fila, una acción destructiva (Eliminar) y un error que impidió guardar o cargar (`FormError`, `ErrorNotice`). Nada más es clay. Todo lo demás que antes era "esto necesita tu atención" (una valuación vieja, un vencimiento atrasado, un dato que falta) usa `--color-attention`, un ocre apagado, SOLO como un punto de ~8px redondo al lado de un nombre — nunca rellena un bloque ni pinta texto largo. Un vencimiento atrasado (`DueReminder`) ya no tiñe su tarjeta: escala con el punto y el texto en `ink` semibold ("venció hace N días"), no con color de fondo.
- **Dos monedas en una tarjeta son dos hechos pares.** Cuando un total tiene saldo en más de una moneda, los montos van del MISMO tamaño, uno debajo del otro, cada uno con su símbolo (`MoneyStack`), y el chip de moneda del encabezado desaparece — cada monto ya dice cuál es. Poner el segundo más chico afirmaría una jerarquía que la app no puede sostener. Y una moneda extranjera en CERO no se muestra: es el caso normal, así que con datos solo en pesos todo se ve exactamente igual que antes (ver ADR-015). En Inicio el chip no aparece nunca, ni con una sola moneda: las filas de los tres mundos (`WorldRow`, `Dashboard.jsx`) son nombre + monto + chevron, sin encabezado propio que lo sostenga.
- **Los montos NO van en monoespaciada.** Van en la tipografía del sistema con cifras tabulares: la clase `font-money` conserva el nombre pero ahora resuelve a la sans + `font-variant-numeric: tabular-nums`. Una monoespaciada le daba a cada número un aire de planilla.
- `Money` (`src/components/Money.jsx`) es el monto PROTAGONISTA de una tarjeta (28px para arriba): símbolo chico y apagado, entero grande, decimales al 0,58em. A 17px el símbolo quedaría en 8px, así que en las filas de una lista va el monto derecho, con `formatUSD`/`formatARS`.
- Clases compartidas en `@layer components` de `index.css`, en vez de repetir la misma tira de utilidades: `surface` (tarjeta), `list` (tarjeta con separadores sangrados entre filas), `rows` (esos separadores sin la tarjeta), `row` (fila de lista: etiqueta y valor, 44px mínimo), `pressable` (el fondo de presionado de algo que se toca), `notice` (aviso teñido en clay — reservado para un error o una acción destructiva, ver la regla del rojo arriba), `callout` (nota neutra: todo lo que "necesita tu atención" sin ser un error va acá), `badge` (marca corta junto a un nombre), `input-inline` (campo dentro de una fila) y `field` (campo relleno suelto), `value-button` (el valor que se toca para cambiarlo: fecha, tipo de cambio, período), `btn` + `btn-primary`/`btn-secondary`/`btn-quiet`/`btn-danger`/`btn-lg`, `btn-text` (botón de texto con blanco táctil de 44px), `fab`, `title-page`, `eyebrow` (encabezado de sección, en minúscula), `page` y `page-narrow`.
- **La diferencia entre celular y desktop vive en un solo lugar**: `.btn` mide 52px de alto con texto de 17px, y baja a 36px con 14px a partir de `md`. Ninguna pantalla repite esas medidas.
- Escala tipográfica como tokens: `text-body` (17, el cuerpo), `text-subhead` (15), `text-footnote` (13), `text-caption` (11), `title-page` para el título de pantalla, y los montos protagonistas — `text-title2` (22, el Total de Inicio), `text-title1` (28, el saldo de una deuda, "Aportado a hoy") y `text-display` (40, el monto grande de una tarjeta de resumen; sube a 44 en desktop con `md:text-[44px]` al lado, no hay un segundo token para eso) — y `text-tabbar` (10, la etiqueta de la barra de pestañas). Nunca un `text-[17px]` suelto: si hace falta un tamaño nuevo, se agrega a `@theme`. El cuerpo de la app es 17px, no 14: esto se lee con el teléfono en la mano.
- Contraste: `ink-soft` pasa AA (4,5:1) sobre fondo, tarjeta y `mist`; `ink-faint` es solo para lo gráfico o decorativo (chevrones, placeholders). Texto que informa nunca va en faint.
- Movimiento: una curva (`--ease-ios`) y tres duraciones (`--duration-fast/base/slow`) en `@theme` para transiciones CSS. `src/lib/spring.js` es la primitiva de todo movimiento GUIADO POR UN GESTO (un resorte amortiguado con `damping`/`response`, sin dependencias, y la función `project` para la posición proyectada de un impulso): la usa `FormSheet` (bloque 11) y la reutilizan reordenar (bloque 12) y volver deslizando (bloque 13). Todo lo que se mueve responde a algo que hizo el usuario, y `prefers-reduced-motion` lo apaga globalmente. Los cambios de pantalla usan View Transitions (`viewTransition` en cada `Link`/`navigate`); la barra de pestañas y la columna lateral tienen `view-transition-name` propio y no participan. La fila recién guardada en Movimientos se ilumina una vez con `.animate-highlight` (un fondo `accent` al ~10% que se apaga solo en 1,2s) — el único movimiento del bloque 05, además del fundido de los esqueletos.
- **Esqueletos (bloque 05)**: cada pantalla dibuja el suyo con sus propias piezas —las MISMAS clases que el contenido real (`surface`, `list`, `row`, `eyebrow`)— con un `.placeholder` (`index.css`) en vez del texto: un bloque quieto en `mist`, del alto de la línea que reemplaza. Nunca `animate-pulse` (nada late); aparece recién a los ~150ms con un fundido (`animation-delay`, no un timer de JS), así en una red buena no llega a verse. `loading`, en los hooks de `hooks/`, es siempre "no hay nada que mostrar todavía" (la primera carga sin caché) y nunca "está refrescando" — volver a una pantalla ya visitada, o guardar algo, no vuelve a mostrar el esqueleto: los datos de antes se quedan quietos hasta que llega lo nuevo, que los reemplaza en su lugar. Ver `Movements.jsx`, `Portfolio.jsx`, `Commitments.jsx`, `Debts.jsx` y `settings/Accounts.jsx` para el patrón.
- Íconos: todos en `components/Icons.jsx`, nunca un `<svg>` suelto en una pantalla.
- Anchos: `page` abre a una grilla ancha en desktop (Inicio, Inversiones, Movimientos, Deudas, detalle de activo); `page-narrow` se queda angosta a propósito (Ajustes, Mi plata, login, Objetivo). Desktop no es la columna del celular estirada: Inicio pone el resumen en tres columnas y el gráfico al lado de los gastos, Movimientos parte en dos paneles, el detalle de activo pone el historial a la derecha.
- La acción principal en el celular es el botón flotante "+" (`fab`); en desktop es un botón normal en el encabezado (`action` de `PageHeader`) y el flotante se oculta. Un FAB no tiene sentido con un mouse.
- Navegación del celular: cinco pestañas, todas plata (Inicio, Movimientos, Mi plata, Inversiones, A pagar). Ajustes no es una pestaña: se entra desde el botón de Inicio, y en desktop vive al pie de la columna lateral. Toda subpantalla lleva su volver en la barra de `PageHeader` (ver abajo), nunca un link suelto arriba del título.
- **Volver retrocede de verdad** (`useGoBack`): si hay una entrada anterior dentro de la app, un "volver" la consume (no agrega historia); si no la hay —link directo, PWA reabierta en esa ruta—, reemplaza a la pantalla madre. El atrás del sistema (Android, el gesto de iOS) nunca reabre una pantalla que ya se cerró. Cambiar de pestaña también reemplaza la entrada actual (nunca apila), salvo salir de Inicio, que sí agrega una — así el atrás, desde la raíz de cualquier pestaña, va a Inicio y de ahí sale de la app. Después de eliminar algo (una cuenta, un plan, una tarjeta, una categoría, un grupo) la pantalla vuelve con el mismo `useGoBack`, nunca con un `navigate` a una ruta fija.
- **La barra superior fija** (celular, `PageHeader.jsx`): pegada arriba con la zona segura del dispositivo (`env(safe-area-inset-top)`), vacía y transparente arriba de todo; cuando el título grande sale por arriba (un `IntersectionObserver`, no un listener de `scroll`), aparecen el título chico centrado, el fondo translúcido con blur y un borde inferior. El volver vive siempre ahí, a la izquierda, con el rótulo de a dónde vuelve. La acción de la pantalla (`action` de `PageHeader`) vive siempre en esta barra en el celular, y al lado del título grande en desktop, que no tiene barra — el propio contenido de `action` decide qué mostrar en cada ancho con sus propias clases `md:`.
- **Zonas seguras**: los laterales del contenido son el mayor entre 16px y `env(safe-area-inset-left/right)`; el de arriba lo da la barra fija de `PageHeader`, no un padding fijo. `FormSheet` suma la zona segura de arriba a su encabezado solo cuando está expandido a pantalla completa. No cambiar `apple-mobile-web-app-status-bar-style` en `index.html` (hoy `default`): en la PWA la zona segura de arriba da 0 y no se nota nada; es para el empaquetado a pantalla completa futuro.
- **Transiciones con sentido**: cambiar de pestaña es instantáneo (sin `viewTransition` en esos `NavLink`, ver `Layout.jsx`). Entrar a un detalle trae la pantalla nueva desde la derecha, empujando a la vieja hacia la izquierda con un oscurecido apenas; volver es el camino inverso. `useGoBack` marca la dirección (`data-nav="back"` en el documento) antes de navegar — los keyframes de ida y de vuelta viven en `index.css`. El atrás del sistema (un POP) nunca anima: React Router no envuelve un POP en una View Transition, así que `useGoBack` dispara la suya a mano solo para el volver de la propia app (ver el comentario en `useGoBack.js`). La barra de pestañas, la columna lateral, el FAB y la barra de acciones del detalle de un activo tienen `view-transition-name` propio: no viajan con la pantalla.
- Los gráficos (recharts) pintan en SVG y no entienden clases: leen los colores del DOM con `readChartColors()` (`src/lib/chartColors.js`), memorizado contra `accent` e `isDark`. Nunca copiar hex a un archivo de gráfico.

## Convenciones de formularios
- Cada campo se nombra con la pregunta que responde, en el idioma del usuario — nunca desde la implementación. Nada de nombres que asuman conocimiento del sistema ("Va por MEP") ni que nombren flags internos ("Ya lo tenía"). Las ayudas se escriben para alguien que usa la app por primera vez.
- Segmentado (`BinaryChoice`, en `src/components/form/`) para elegir entre modos, cuando la operación cambia de naturaleza (ej. Gasto/Ingreso). Switch (`Switch`, mismo directorio) para un ajuste sí/no que no transforma la operación.
- Eliminar (permanente): botón rojo + confirmación que dice explícitamente "es permanente". Archivar (reversible): botón neutro + confirmación suave, sin esa palabra.
- Botón primario de los modales: siempre "Guardar", salvo un verbo explícito que describa mejor la acción (ej. "Liquidar" en la liquidación de una posición — no es un guardado genérico, es vender).
- **Guardar sin tocar nada deja la fila idéntica.** Vale para todo formulario de edición y es la regla que ordena el campo de tipo de cambio: la tasa guardada la siembra el formulario padre en su reseteo, `ExchangeRateField` en modo edición NO reporta nada al montar (los efectos de los hijos corren antes que los del padre, así que el reseteo pisaba lo que el hijo reportaba), y una fila guardada sin tasa no sale a buscar el MEP de hoy — se ofrece cargarla a mano, que es intervención explícita. Mismo criterio con cualquier insumo de un cálculo congelado: `empties_asset` se lee de la fila al editar un retiro, no se asume (ver ADR-011).
- **Pesos y dólares se cargan lado a lado**, con el mismo peso visual, y se derivan entre sí con el tipo de cambio vigente (bidireccional, manda el último tocado — mismo criterio que `QuantityAmountField`). Lo que se guarda no cambia: `amount_usd` + `mep_rate`; los pesos son un campo de entrada, no una columna. Cuando el usuario cambia la tasa a mano, los dos montos dejan de cuadrar: se pregunta cuál vale mostrando los dos importes concretos, en vez de recalcular a ciegas.
- Errores: siempre con `FormError` (`src/components/form/`) — mensaje en español + detalle opcional. Al `detail` se le pasa **el error, no un texto**: qué parte de él puede leer una persona lo decide `describeError` (`src/lib/errors.js`), en un solo lugar. El texto de una restricción de la base NUNCA llega a la pantalla; un error que escribimos nosotros para el usuario se tira con `UserError` (misma regla del lado de Postgres: nuestras funciones avisan en castellano con `raise exception`, y eso sí se muestra).
- Todo modal de formulario usa el componente `FormSheet` (`src/components/FormSheet.jsx`), nunca su propia caja `fixed`. En mobile abre como bottom sheet compacto (alto natural hasta `85dvh`, anclado abajo, esquinas superiores redondeadas) SIN teclado, y al enfocar un campo se expande a pantalla completa (`h-dvh`, header fijo + cuerpo scrolleable) subiendo el campo por encima del teclado. Los formularios que autoenfocan (Movimientos) pasan `startExpanded` para abrir ya expandidos con teclado, sin parpadeo. En desktop es una card centrada con alto acotado y scroll interno (compacto/expandido no aplica). El botón de acción (submit) se pasa como prop `action` y usa `form="<id>"` para enviar el `<form>` del cuerpo. NO detectar el teclado on-screen para acomodar el sheet: en PWA standalone de iOS el evento `resize` de `visualViewport` es poco confiable (bug de WebKit sin arreglar) y `interactive-widget` del viewport meta no lo soporta Safari en ninguna versión — por eso la expansión a full-screen es el mecanismo, no medir el teclado. Respeta `env(safe-area-inset-bottom)`. Una barra fija inferior no-modal (ej. la de acciones del detalle de activo) sigue la misma regla de `safe-area-inset-bottom`.
- **`FormSheet` es dueño de su propia presencia (bloque 11).** Recibe `open` y lo pasa un solo booleano: con `true` se monta y entra con el resorte de `src/lib/spring.js`; con `false` estando visible SALE con el resorte y recién al terminar se desmonta — quien lo llama nunca lo desmonta a mano. Por eso **ningún modal hace `if (!open) return null`**: los 15 formularios pasan `open` directo a `FormSheet` y solo dejan de renderizar su CONTENIDO cuando falta el dato que necesitan (`!asset`, `!debt`), nunca por `!open` — si `open` decidiera el return, `FormSheet` se desmontaría de un salto y no podría animar la salida. Los padres que antes los montaban condicionalmente (`{x && <Modal/>}`) ahora los montan siempre. Mientras sale, el contenido queda CONGELADO (el último `title`/`children`/`onSubmit` que llegó con `open` en `true`): el padre puede limpiar su estado al cerrar (`setEditing(null)`) sin que el título salte de "Editar…" a "Nuevo…" a mitad de la animación. Con el dedo (`pointerType 'touch'`) se agarra de la barrita, del encabezado —también a pantalla completa, incluido el formulario de gasto— y del cuerpo cuando ya está scrolleado arriba de todo; sigue 1:1 al dedo, decide cerrar o volver con la posición proyectada (`project`, la fórmula de UIScrollView) contra la mitad del alto del panel, y en cualquiera de los dos casos arranca con la velocidad real del gesto. Un arrastre puede interrumpir cualquier resorte en curso (entrando, saliendo, o el de otro arrastre) y seguir desde ahí. En desktop no hay nada de esto: la tarjeta centrada solo entra/sale con el resorte, sin arrastre. Con `prefers-reduced-motion` no hay resorte, solo un fundido corto.
- Recuperación de contraseña: el link de Supabase no apunta a una ruta propia — cae en el Site URL con los tokens en el hash de la URL. `lib/supabase.js` los parsea de forma SINCRÓNICA, antes de crear el cliente, porque el evento `PASSWORD_RECOVERY` de supabase-js puede llegar tarde. Mientras hay una recuperación en curso, `App.jsx` no monta las rutas normales: todo redirige a `ResetPassword`.
- Al enfocar un campo, FormSheet lo scrollea al tope del cuerpo (`block: 'start'`) para dejarlo por encima del teclado. Esto es OBLIGATORIO, no una mejora: en PWA standalone de iOS WebKit no auto-scrollea un contenedor anidado (`overflow-y-auto`) para revelar el campo enfocado, solo el documento raíz — sin este scroll el teclado tapa cualquier campo de la mitad de abajo. Criterio de aceptación de cualquier form: el campo enfocado siempre visible por encima del teclado, nunca tapado.
- Autofocus SOLO en los formularios de Movimientos (captura rápida de gasto/ingreso). Todos los demás formularios (Aportar, Retirar, Transferir, Liquidar, Valuación, alta/edición de activo, reconciliación de líquido) abren SIN teclado: el usuario toca el campo cuando quiere. Excepción: un input que se revela por una acción explícita del usuario (ej. el date picker de `CollapsedDateField` tras tocar "cambiar", o los inputs inline de Ajustes) puede autoenfocarse — no es "abrir el form con teclado".

## Comandos
- `npm run dev` — servidor de desarrollo (Vite)
- `npm run build` — build de producción
- `npm run preview` — sirve el build localmente
- `npm run lint` — linter (oxlint)
- `npm run verify:rls` — verifica el aislamiento RLS con el usuario test (requiere `.env.test.local`, ver `.env.test.example`)

## Cómo probar
- Copiá `.env.test.example` a `.env.test.local` y completá `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` con el usuario test (ese archivo lo creás vos a mano, nunca por acá).
- `npm run verify:rls` corre solo lectura contra Supabase y confirma que RLS aísla los datos por usuario.
- El login manual en `npm run dev` se hace con ese mismo usuario test.
- La verificación de datos con `npm run verify:rls` se corre siempre antes de proponer commit.
- La verificación visual con navegador queda reservada para cambios grandes de UI o cuando el usuario lo pida explícitamente; por defecto la hace el usuario manualmente. Cuando se use navegador, exclusivamente Playwright MCP — nunca Claude in Chrome ni el navegador personal del usuario. Nunca incluir credenciales en output, código ni screenshots.
