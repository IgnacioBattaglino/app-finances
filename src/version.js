// Versión de la app, tomada de package.json e inyectada en build por Vite
// (define __APP_VERSION__ en vite.config.js). Fuente única: para subir la
// versión, editá "version" en package.json. Se muestra en Inicio para poder
// confirmar a ojo si un deploy se aplicó. 1.0.0 será el MVP.
export const APP_VERSION = __APP_VERSION__
